This folder contains the first task for the **alx_html_css** project.  
The goal is to introduce basic styling using separate CSS files.

## Files Included

- `index.html`
- `tweets.html`
- `base.css` – Contains basic global styles.
- `styles.css` – Custom stylesheet (currently empty).
- `README.md`

## Instructions Implemented

1. Copied `index.html` and `tweets.html` from the previous `html_basic` project.
2. Created new CSS files: `base.css` and `styles.css`.
3. Added the following lines inside the `<head>` tag of each HTML file:

```html
<link href="base.css" rel="stylesheet">
<link href="styles.css" rel="stylesheet">
